{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Dependency Injection Cat configuration schema",
  "additionalItems": false,
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "unsafeTSVersion": {
      "type": "boolean",
      "default": false,
      "title": "Allows to use TypeScript version that is not officially supported by this version of dependency-injection-cat",
      "examples": [true, false]
    }
  }
}
